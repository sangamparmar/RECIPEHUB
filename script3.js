    function scaleCategory(card) {
      card.style.transform = 'scale(1.1)';
      card.style.transition = 'transform 0.3s ease';
    }

    function resetCategory(card) {
      card.style.transform = 'scale(1)';
    }
